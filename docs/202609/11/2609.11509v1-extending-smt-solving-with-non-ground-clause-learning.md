---
title: Extending SMT Solving with Non-Ground Clause Learning
title_zh: 用非基子句学习扩展SMT求解
authors: "Yasmine Briefs, Christoph Weidenbach"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11509v1"
tags: ["query:cdcl-sat"]
score: 7.0
evidence: CDCL(T)风格SMT求解中的非基冲突分析与子句学习
tldr: 非ground SMT求解目前主要依靠量词实例化：求解器先产生ground实例，再用CDCL(T)风格推理求解，但冲突分析只学习ground子句，即使冲突实际来自非ground子句的实例。本文提出一种演算，把ground实例化、CDCL(T)规则和非ground冲突分析统一起来：求解仍在ground实例上进行，冲突分析的分辨步骤却直接作用于原始非ground子句。这样学到的子句通常比ground冲突更一般，在合适策略下甚至非冗余，并且能够加入时序回溯。该演算为CDCL(T)式SMT、多种实例化过程和非ground子句学习提供共同框架，并可模拟CDCL、SCL(FOL)、SCL(T)乃至Resolution。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11509v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.11509v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 851, \"label\": \"Table\"}]"
motivation: 现有非ground SMT求解虽用CDCL(T)处理ground实例，但冲突分析只学习ground子句，丢失非ground推理可带来的指数级短证明优势。
method: 提出一种演算，结合ground实例化、CDCL(T)规则与非ground冲突分析；求解在ground实例上，但分辨作用于原始非ground子句。
result: 学到的子句通常比ground冲突更一般，合适策略下甚至非冗余；演算支持时序回溯，并可模拟CDCL、SCL(FOL)、SCL(T)和Resolution。
conclusion: 该演算统一CDCL(T)式SMT、实例化过程与非ground子句学习，为更短证明和非冗余学习提供共同框架。
---

## 摘要
量词实例化目前是非基SMT求解的主要方法：求解器生成基实例，并以CDCL(T)风格推理求解所得的基SMT问题。当发现冲突时，冲突分析只学习一个基子句，即使该冲突来自非基子句的实例。然而，非基推理可以给出比纯基推理指数级更短的证明。我们提出一种演算，它由基实例化、CDCL(T)风格规则和非基冲突分析组成。求解器在基实例上进行推理，但冲突分析的归结步骤是在其原始非基子句上执行的。这会生成通常比基冲突更一般的学习子句。在合适的策略下，学习到的子句甚至是非冗余的。我们还展示了如何将时序回溯纳入SMT求解。我们的演算为CDCL(T)风格SMT求解、一系列基于实例化的过程以及非基子句学习提供了一个统一框架，并且我们证明它模拟了CDCL、SCL(FOL)、SCL(T)，甚至归结。

## Abstract
Quantifier instantiation is currently the main approach to non-ground SMT solving: solvers generate ground instances and solve the resulting ground SMT problems with CDCL(T)-style reasoning. When a conflict is found, conflict analysis learns only a ground clause, even though the conflict comes from instances of non-ground clauses. Yet non-ground reasoning can give exponentially shorter proofs than purely ground reasoning. We propose a calculus that consists of ground instantiations, CDCL(T)-style rules, and non-ground conflict analysis. The solver reasons on ground instances, but the resolution steps of conflict analysis are performed on their original non-ground clauses. This produces learned clauses that are typically more general than the ground conflict. With a suitable strategy, the learned clauses are even non-redundant. We also show how chronological backtracking can be included in SMT solving. Our calculus gives a common setting for CDCL(T)-style SMT solving, a range of instantiation-based procedures, and non-ground clause learning, and we prove that it simulates CDCL, SCL(FOL), SCL(T), and even Resolution.