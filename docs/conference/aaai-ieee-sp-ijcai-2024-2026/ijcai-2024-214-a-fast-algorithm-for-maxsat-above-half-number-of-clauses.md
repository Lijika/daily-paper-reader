---
title: A Fast Algorithm for MaxSAT above Half Number of Clauses
title_zh: 超过半数子句的最大可满足性问题的快速算法
authors: "(PDF |   Details)"
date: 2024-08-01
pdf: "https://www.ijcai.org/proceedings/2024/0214.pdf"
tags: ["query:sat-bva-cnf"]
score: 6.0
evidence: MaxSAT求解算法
tldr: 本文提出一种针对超过半数子句可满足的MaxSAT问题的快速算法，通过参数化方法改进上界，为MaxSAT求解提供了更高效的算法基础。
source: IJCAI-2024-Accepted
selection_source: conference_retrieval
figures_json: "[{\"url\": \"assets/figures/ijcai-2024-accepted/ijcai-2024-214/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 848, \"height\": 326, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/ijcai-2024-accepted/ijcai-2024-214/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 772, \"height\": 651, \"label\": \"Table\"}]"
motivation: 提高MaxSAT问题在特定条件下的求解效率。
method: 设计参数化算法，利用子句数边界。
result: 实现了更优的算法复杂度上界。
conclusion: 该算法在理论上推进了MaxSAT的求解效率。
---

## Abstract
No abstract is available.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：MaxSAT（最大可满足性问题）的“超过保证值”参数化版本（称为MaxSAT-AH），即给定一个CNF公式F（含有m个子句），判断是否存在一个赋值使得至少满足⌈m/2⌉ + μ个子句，其中μ是参数。该问题的研究动机源于平凡的上下界：总是存在赋值满足至少⌈m/2⌉个子句（全真或全假赋值），因此只有当μ>0时问题才有意义。
- **背景**：自Mahajan和Raman（1999）提出该参数化以来，“above guarantee”参数化成为活跃的研究方向。此前该问题的最好时间复杂度为O*(5.4064^μ)，由已知的O*(1.3248^k)算法通过转化得到（其中k是目标满足子句数）。论文指出，过去二十多年所有针对μ的改进都源自对k算法的改进，而本文首次提出一种专门针对μ的参数化算法，打破了这一范式。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：采用分支限界（branch-and-bound）范式，设计新的归约规则和分支规则，使得参数μ（= k – m/2）在分支过程中能够有效减少，从而得到更优的运行时间上界。
- **关键技术细节**：
  1. **六个归约规则（R-Rules 1–6）**：前四个为已知规则（如删除互补单位子句、分辨率规则等），后两个是本文新提出的关键规则（R-Rule 5和R-Rule 6），用于处理某些特殊结构，确保在分支时参数μ不会增加，并且能够高效减少。
  2. **分支步骤**：算法按顺序执行六个分支步骤（B-Rules 1–8），每一步处理特定类型的文字（literals）或子句。例如：
     - Step 1：处理6次以上出现的变量或(4,1)-文字，分支后得到向量(1.5, 0.5)（即两个分支分别减少μ至少1.5和0.5），对应分支因子2.1479。
     - Step 2：处理(3,2)-文字，得到类似向量。
     - Step 3：处理(2+,1)-非单例文字，得到(1,1)向量。
     - Step 4：处理(2,2)-文字和(2,1)-单例文字，最坏情况(0.5, 1.5)（瓶颈情况）。
     - Step 5：处理短子句（长度2或3）中的(3,1)-单例文字，最坏情况(1,1.5)或(1,1.5,2)，对应分支因子仍不超过2.1479。
     - Step 6：当剩余公式仅包含(3,1)-单例文字且子句长度≥4时，将问题归约为最小集合覆盖（MSC）问题，利用van Rooij和Bodlaender（2011）的精确算法在O*(2.1467^μ)时间内求解。
  3. **测量函数**：使用μ = k – m/2（忽略取整常数），当μ≤0时可直接回答YES。
- **算法流程**：迭代应用归约规则得到简化公式；检查μ≤0则终止；否则依次执行分支步骤。每一步之前均先归约。整个算法的最大分支因子为2.1479，因此总复杂度O*(2.1479^μ)。

### 3. 实验设计

- **无实验**：本文是纯理论算法研究，未提供任何实验验证。论文没有设计数据集、benchmark，也没有对比其他方法（仅有理论上的复杂度比较，如图1展示了与现有算法在参数r=k/m下的运行时间基线的对比）。

### 4. 资源与算力

- **未提及**：由于无实验，论文未涉及任何算力消耗（GPU型号、数量、训练时长等）。

### 5. 实验数量与充分性

- **不适用**：本文无实验。从理论角度，算法正确性和复杂度通过数学证明（引理9-22等）建立，所有分支向量和分支因子均经过严格推导。因此，论文在理论层面是充分的，但缺乏实证评估。

### 6. 论文的主要结论与发现

- 主要结论：MaxSAT-AH问题可以在O*(2.1479^μ)时间和多项式空间内求解，显著优于此前最好上界O*(5.4064^μ)。
- 发现：新提出的归约规则R-Rule 5和R-Rule 6能够有效克服参数μ在分支中不减少的结构障碍，使得专门针对μ的分支算法成为可能。
- 意义：这是二十多年来首个不依赖k算法而直接改进μ的算法，为“above guarantee”参数化提供了新的范式。

### 7. 优点

- **理论创新**：首次设计出专门针对参数μ的算法，打破了以往只能通过转换k算法来改进μ的局限。
- **归约规则设计巧妙**：R-Rule 5和R-Rule 6针对非单例文字和特定结构，保证了参数μ在分支中的有效减少，从而得到更优的分支因子。
- **结合MSC归约**：在最后一步巧妙地将剩余情况归约为最小集合覆盖问题，利用了MSC的已有精确算法，避免了瓶颈。
- **形式化证明完整**：所有分支向量和分支因子均给出详细推导，结果严格。

### 8. 不足与局限

- **缺乏实验验证**：作为纯理论工作，算法在真实应用场景中的实际效率未知。论文未提供任何实现或基准测试，无法评估其实际可行性。
- **应用限制**：算法参数化于μ = k – m/2，仅适用于k接近m/2的情况（小μ）。当μ较大时（即要求满足超过半数的子句数较多），该算法可能不如专门针对k或m的算法高效（如图1所示，当r>0.791时其他算法更优）。
- **归约规则可能不适用于所有实例**：新规则依赖特定结构条件，若输入公式不满足条件则无法应用，可能需要更多分支。
- **理论最优性未证明**：作者未证明2.1479是下界，或许存在进一步改进的空间。

（完）
